import SwiftUI

/// Budget › Recurring (spec §24.2). Filled in by Sprint 2 chunk C.
struct RecurringListView: View {
    var body: some View {
        ContentUnavailableView("No recurring items", systemImage: "arrow.triangle.2.circlepath")
    }
}
