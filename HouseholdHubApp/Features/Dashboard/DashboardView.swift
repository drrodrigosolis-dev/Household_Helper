import SwiftUI

struct DashboardView: View {
    var body: some View {
        NavigationStack {
            FeaturePlaceholder(title: "Dashboard", message: "Nothing here yet", systemImage: "house")
        }
    }
}

#Preview {
    DashboardView()
}
